---
applications:
  .:
    name: weixinechotest
    framework:
      name: rails3
      info:
        mem: 256M
        description: Rails Application
        exec: 
    url: ${name}.${target-base}
    mem: 256M
    instances: 1
    services:
      mysql-12e4c:
        type: mysql
